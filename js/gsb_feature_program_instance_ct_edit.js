(function ($) {

/**
 * Handles changes to the Instance Date state.
 */
Drupal.behaviors.gsbProgramInstanceEdit = {
  attach: function (context) {

   // We now handle changes to Instance Date and Instance Text
  // in the form submit.

  }
};

}(jQuery));
